@model CentroCostos.Models.ViewModels.CategoriaViewModel

<div class="span12">
    <div class="widget">
        <div class="widget-header">
            <i class="icon-bookmark"></i>
            <h3>Editar Categoria</h3>
        </div>
        <!-- /widget-header -->
        <div class="widget-content">
            @using (Html.BeginForm("EditarCategoria", "Administracion", FormMethod.Post, new { @class = "form-horizontal" }))
            {
                @Html.Partial("_ValidationSummary")
                @Html.HiddenFor(x => x.Id)
                
                <fieldset>
                    <div class="control-group">
                        <label class="control-label" for="firstname">Categoria</label>
                        <div class="controls">
                            @Html.TextBoxFor(t => t.Categoria, new { @class = "span6" })
                        </div> <!-- /controls -->
                    </div>

                    <div class="form-actions">
                        <button type="submit" class="btn btn-primary">Guardar</button>
                        <a href="@Url.Action("Materiales", "Administracion")" class="btn">Cancelar</a>
                    </div>
                </fieldset>
            }
        </div>
        <!-- /widget-content -->
    </div>
</div>


