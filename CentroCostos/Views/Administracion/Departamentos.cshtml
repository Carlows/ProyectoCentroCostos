@model CentroCostos.Models.ViewModels.DepartamentosAdmViewModel
@using CentroCostos.Models
@Html.Partial("_TempMessage")

<div class="span7 clearmarginleft">
    <div class="widget">
        <div class="widget-header">
            <i class="icon-list-alt"></i>
            <h3>Departamentos</h3>
        </div>
        <!-- /widget-header -->
        <div class="widget-content">
            @if (Model.Departamentos.Count == 0)
            {
                <div class="alert alert-info">Aún no existe ningun departamento.</div>
            }
            else
            {
                <table class="table table-striped table-bordered">
                    <thead>
                        <tr>
                            <th> Departamento </th>
                            <th class="td-actions"> </th>
                        </tr>
                    </thead>
                    <tbody>
                        @foreach (DepartamentoProduccion departamento in Model.Departamentos)
                        {
                            <tr>
                                <td class="span6">@departamento.Nombre_Departamento</td>
                                <td class="td-actions span2">
                                    <a href="@Url.Action("EditarDepartamento", "Administracion", new { id = @departamento.Id })" class="btn btn-small btn-primary"><i class="btn-icon-only icon-search"> </i></a>
                                    <a href="javascript:;" class="btn btn-danger btn-small"><i class="btn-icon-only icon-remove"> </i></a>
                                </td>
                            </tr>
                        }
                    </tbody>
                </table>

            }
        </div>
        <!-- /widget-content -->
    </div>
</div>
<div class="span5">
    <div class="widget">
        <div class="widget-header">
            <i class="icon-bookmark"></i>
            <h3>Opciones</h3>
        </div>
        <!-- /widget-header -->
        <div class="widget-content">
            <div class="shortcuts">
                <a href="@Url.Action("NuevoDepartamento", "Administracion")" class="shortcut"><i class="shortcut-icon icon-pencil"></i><span class="shortcut-label">Agregar Departamento</span></a>
                <a href="javascript:;" class="shortcut"><i class="shortcut-icon icon-eraser"></i><span class="shortcut-label">Importar datos</span></a>
            </div>
            <!-- /shortcuts -->
        </div>
        <!-- /widget-content -->
    </div>

</div>